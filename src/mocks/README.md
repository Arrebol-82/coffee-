# Mocks

MSW setup and mock API modules for local development and demo deployments.

`browser.ts` starts the worker, `handlers.ts` combines handlers, and `modules/` contains feature-level mock data behavior.
